<% unless params[:preview] == 'yup' -%>
<h1 class="posts icon"><%= @post.name %></h1>
<% end -%>
<%= form_for(@post, :url => admin_post_path(@post)) do |form| %>
  <%= render :partial => 'form', :locals => {:form => form} %>
  <div class="toolbar">
    <%= button 'Save' %>
    <%= button_link_to('Cancel', return_path || admin_posts_path, :class => 'cancel') %>
  </div>
<% end =%>
